if ( $('#edit-folder-modal').length === 0) {
<% if params[:subaction] %>
sakve.modal('edit-folder-modal',
    '<%= j render("folders/edit/#{params[:subaction]}") %>');
<% else %>
sakve.modal('edit-folder-modal',
    '<%= j render("form") %>');
<% end %>
}
