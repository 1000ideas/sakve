<%= form_for(@tag) do |f| %>
<%= render partial: 'l/partials/errors', object: @tag.errors %>
<div class="group">
      <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name  %>
    </div>
  </div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
