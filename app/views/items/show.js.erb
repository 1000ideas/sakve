$('<div data-reveal>')
  .addClass('reveal-modal')
  .html("<%= j render('show') %>")
  .appendTo('body')
  .foundation()
  .on('closed', function() { $(this).remove(); })
  .foundation('reveal', 'open')
