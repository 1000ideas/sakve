$('<div data-reveal>')
  .addClass('reveal-modal small')
  .html("<%= j render('form') %>")
  .appendTo('body')
  .foundation()
  .on('closed', function() { $(this).remove(); })
  .foundation('reveal', 'open')
  .find('.tags-autocomplete')
  .autocomplete(Sakve.tags_autocomplete_options);
