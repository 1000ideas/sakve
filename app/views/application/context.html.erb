<ul class="actions">
  <% if @item %>
    <% name = @item.class.name.downcase %>
    <%= render "context_#{name}", :"#{name}" => @item %>
  <% else %>
    <%= render "context_bulk" %>
  <% end %>
</ul>
