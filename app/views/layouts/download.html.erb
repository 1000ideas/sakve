<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "1000ideas Sakve" %></title>
    <%= tag(:link, rel: :icon, type: "image/png", href: '/favicon.png') %>

    <%= stylesheet_link_tag    "download" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="cover" data-cover="<%= image_path("transfer/cover-#{(1..7).to_a.shuffle.first}.jpg") %>"></div>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
