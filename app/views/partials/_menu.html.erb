<header>
  <div class="row collpase">
    <hgroup class="hide-for-small medium-3 large-3 columns">
      <h1>Sakve</h1>
    </hgroup>
    <div class="small-12 medium-9 large-9 columns">
      <div class="row">
        <nav class="small-8 medium-8 large-8 columns main-menu">
          <ul>
            <%= content_tag :li, link_to(t('.items'), items_path) if can? :read, Item %>
            <%= content_tag :li, link_to(t('.transfer'), transfers_path) if can? :create, Transfer %>
            <%= content_tag :li, link_to(t('.groups'), groups_path) if can? :manage, Group %>
          </ul>
        </nav>
        <nav class="small-4 medium-4 large-4 columns text-right">
          <ul>
            <%= content_tag :li, link_to(current_user.email, edit_user_registration_path) if current_user %>
            <%= content_tag :li, link_to(t('.logout'), destroy_user_session_path, method: :delete) if current_user %>
          </ul>
        </nav>
      </div>
      <div class="row">
        <div class="columns small-12 medium-12 large-12">
          <%= yield :buttons %>
        </div>
      </div>
    </div>
  </div>
<!-- <ul>
  <% (I18n.available_locales - [I18n.locale]).each do |lang| %>
    <%= content_tag :li, link_to("Switch to #{lang}", switch_lang_path(lang: lang)) %>
  <% end %>
</ul>
 --></header>
